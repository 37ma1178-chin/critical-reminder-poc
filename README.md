# Urbania configurator — starter

Open this folder in Claude Code and start with:

> Read CLAUDE.md, then do task 1: load data/urbania-17.json in both index.html and topview.html and remove every hardcoded dimension. Run a static server, screenshot both pages, and show me they still render identically to before.

Then feed it tasks 2–5 one at a time.
